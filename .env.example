# Comma-separated list of shell origins this MFE accepts commands from.
# Used at build time via Vite to pre-fill src/config.ts in the future.
# For now config.ts has the dev value hard-coded.
VITE_ALLOWED_SHELL_ORIGINS=http://localhost:3000
